# Copy to .env (gitignored) and fill in. docker-compose reads these.

# MinIO root credentials (used by docker-compose and as the DVC S3 keys).
# Must match .dvc/config.local. Defaults to minioadmin/minioadmin if unset.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
