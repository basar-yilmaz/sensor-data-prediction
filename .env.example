# Copy to .env (gitignored) and fill in. Direct dataset downloads use remote MinIO.

# Read-only remote MinIO credentials scoped to the dataset bucket.
MINIO_ENDPOINT_URL=https://s3.basaryilmaz.com
MINIO_ROOT_USER=readonly
MINIO_ROOT_PASSWORD=LX4NLJN8tKc7t8gZAFw9wSAijQ3EwzD

# DVC uses .dvc/config credentials, not the read-only dataset credentials above.
# Keep this true to cache locally reproduced artifacts in bfrb-data.
BFRB_DVC_PUSH_DATA=true
