defaults:
  - _self_
  - prepare: default
  - splits: default
  - datamodule: default

auto_prepare: true

download:
  # Direct raw CSV mirror; streamed to disk because the file is large.
  url: https://router.basaryilmaz.com/train.csv
