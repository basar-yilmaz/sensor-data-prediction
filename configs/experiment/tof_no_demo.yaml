# @package _global_
defaults:
  - override /model: temporal_conv_gru_tof

model:
  aux_binary: false

training:
  class_weighting: sqrt_inv_freq
  aux_binary_weight: 0.0
  max_epochs: 30
  batch_size: 64
